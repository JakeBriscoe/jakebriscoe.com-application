name: Web Client CI

on:
  push:
    paths:
      - "packages/web-client/**"
    tags-ignore:
      - "**"

jobs:
  call-base-workflow:
    uses: ./.github/workflows/base-workflow.yaml
    secrets: inherit
    with:
      service-name: web-client
      package-path: packages/web-client/
